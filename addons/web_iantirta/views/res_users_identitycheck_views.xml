<?xml version="1.0" encoding="utf-8"?>
<sigil>
    <data>
        <record id="res_users_identitycheck_view_form" model="ir.ui.view">
            <field name="model">res.users.identitycheck</field>
            <field name="inherit_id" ref="base.res_users_identitycheck_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//form" position="replace">
                    <form string="Password Confirmation">
                        <sheet>
                            <div class="" invisible="auth_method != 'password'">
                                <h3 class="mb-1">
                                    <strong>Let's first confirm it's you.</strong>
                                </h3>
                                <span class="small text-muted">For your security, please confirm your password to continue.</span>
                                <div class="mt-4">
                                    <field class="o_field_highlight w-100" name="password" autocomplete="current-password"
                                        required="auth_method == 'password'" password="True" placeholder="Enter your current password"/>
                                </div>
                                <div class="d-flex justify-content-end align-items-center">
                                    <a href="/web/reset_password/" class="btn btn-link pe-0" role="button">Forgot password?</a>
                                </div>
                            </div>
                        </sheet>
                        <footer>
                            <button string="Discard" special="cancel" data-hotkey="z" class="btn btn-secondary"/>
                            <button string="Confirm Password" id="password_confirm" type="object" name="run_check"
                                class="btn btn-primary" data-hotkey="q" invisible="auth_method != 'password'"
                                context="{'password': password}"/>
                        </footer>
                    </form>
                </xpath>
            </field>
        </record>
    </data>
</sigil>